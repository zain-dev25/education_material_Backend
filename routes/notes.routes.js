import express from "express";
// import { getFilteredNotes } from "../controllers/notes.controllers.js";

const router = express.Router();

// Example: GET /api/v1/notes?grade=10&subject=Math&board=Punjab
// router.get("/", getFilteredNotes);

export default router;
